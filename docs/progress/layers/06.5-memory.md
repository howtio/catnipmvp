# 06.5 Memory Progress

## 2026-05-19 / Phase 6.5 / 设计并插入 Memory 层

### 当前目标

在 `06-skills` 与 `07-runner` 之间正式插入 `06.5-memory`，明确它和 Context、Runner 的边界，避免 session history 继续散落。

### 本次完成

- 创建本层进度日志文件
- 固化本层编号为 `06.5-memory`
- 明确调用顺序为 `Context -> Skills -> Memory -> Runner`
- 明确本层先做 session/run 级短期记忆
- 明确本层负责记忆提取、压缩、注入、回写
- 明确本层不直接读写 workspace、不执行 shell、不调用模型
- 明确不默认接数据库、向量库或外部长期记忆

### 当前状态

- 已完成：文档层面的职责定义和插层定位
- 进行中：Memory 的 TypeScript 接口与 Harness 串联设计
- 未完成：内存存储实现、回写策略、测试覆盖

### 风险与阻塞

- 如果继续把 session history 放在 Context，会和 Memory 职责重叠
- 如果让 Runner 自己缓存历史，会重新破坏分层边界
- 如果一开始就接外部持久化，会把 MVP 复杂度抬得过高

### 下一步

- 建立 `src/layers/06.5-memory`
- 定义输入输出类型和最小内存存储
- 让 Harness 在 Runner 前读取记忆，在 Runner 后回写记忆

## 2026-05-19 / Phase 6.5 / 落地最小 session memory 闭环

### 当前目标

把文档里的 `06.5-memory` 真正接到代码主链路里，先做进程内 session 级短期记忆，不扩到外部持久化。

### 本次完成

- 创建 `src/layers/06.5-memory/types.ts`
- 创建 `src/layers/06.5-memory/wrapper.ts`
- 创建 `src/layers/06.5-memory/index.ts`
- 记忆按 `sessionId` 分桶
- 记忆条目记录 `taskInput`
- 记忆条目记录 `finalAnswer`
- 记忆条目记录 `stepsUsed`
- 记忆条目记录 `toolSummaryCount`
- `hydrateContext` 会把最近记忆注入 `systemPrompt`
- `hydrateContext` 会产出结构化 `memory.summary`
- `rememberRun` 会在每次 run 结束后统一回写
- 支持 `maxEntries` 裁剪旧记忆
- 新增 `tests/memory.test.ts`

### 当前状态

- 已完成：最小 session/run 级短期记忆闭环
- 进行中：记忆摘要仍是规则拼接，不做更细压缩
- 未完成：跨进程持久化、长期记忆、偏好抽取

### 风险与阻塞

- 当前记忆完全在进程内，重启后不会保留
- 当前摘要直接拼接到 `systemPrompt`，长会话下仍需进一步裁剪策略

### 下一步

- 可继续补记忆条目分级和摘要压缩
- 或补失败 run 的更细记忆保留策略
