# 09 Tool Registry Progress

## 2026-05-18 / Phase 0 / 初始化

### 当前目标

建立 Tool Registry 层进度文档，并固定“定义不执行”的边界。

### 本次完成

- 创建本层进度日志文件
- 明确本层只定义工具，不做执行

### 当前状态

- 已完成：进度文档占位
- 进行中：无
- 未完成：tool definition、schema、权限声明

### 风险与阻塞

- 如果工具定义和执行耦合，后续 Executor 会失去边界
- schema 表达方式尚未确定

### 下一步

- 在 Phase 4 中补齐工具注册骨架

## 2026-05-18 / Phase 0 / 建立空代码骨架

### 当前目标

为 Tool Registry 层建立最小可编译的工具定义接口。

### 本次完成

- 创建 Tool Registry 层类型定义
- 创建最小工具列表 wrapper
- 创建 index 导出

### 当前状态

- 已完成：工具定义骨架可编译
- 进行中：仅有默认工具元信息
- 未完成：schema 校验、注册与解析策略

### 风险与阻塞

- 当前默认工具列表还不足以覆盖总文档目标

### 下一步

- 在 Phase 4 中补齐工具定义和解析逻辑

## 2026-05-18 / Phase 3 / 增加最小工具解析接口

### 当前目标

让 Tool Registry 不仅能列工具，还能被 Runner 和 Executor 按名称解析工具。

### 本次完成

- 保留默认工具列表
- 增加 `getTool(name)` 解析能力

### 当前状态

- 已完成：列表与按名解析
- 进行中：工具 schema 仍未落地
- 未完成：动态注册、schema 校验、权限策略细化

### 风险与阻塞

- 当前工具定义仍偏元信息级别

### 下一步

- 在 Phase 4 中继续补权限、schema 和 guard 所需字段

## 2026-05-18 / Phase 4 / 扩展工具元数据骨架

### 当前目标

让 Tool Registry 提供足够的工具元数据，支撑 Executor 的 guard 准入检查。

### 本次完成

- 为工具定义增加 `category`
- 为工具定义增加 `argShape`
- 为工具定义增加 `stage`
- 为 `write_file`、`patch_file`、`shell_exec`、`git_diff` 增加计划态定义

### 当前状态

- 已完成：基础元数据与计划工具占位
- 进行中：schema 仍为摘要级字段
- 未完成：真实参数 schema、动态注册、执行器绑定

### 风险与阻塞

- 当前元数据仍不足以直接驱动真实工具执行

### 下一步

- 在 Phase 5 中把元数据继续细化为真实最小工具实现所需字段

## 2026-05-18 / Phase 5 / 标记第一批活跃只读工具

### 当前目标

让 Tool Registry 中的最小只读工具不再只是计划项，而是与真实执行能力对齐。

### 本次完成

- 将 `list_files` 标记为 `active`
- 将 `read_file` 标记为 `active`
- 将 `git_diff` 标记为 `active`

### 当前状态

- 已完成：第一批只读工具定义与实现状态对齐
- 进行中：写入类和 shell 类工具仍是计划态
- 未完成：真实参数 schema 与执行器绑定描述细化

### 风险与阻塞

- 当前工具定义仍未表达完整参数字段约束

### 下一步

- 在后续阶段继续补参数 schema 和写入类工具定义

## 2026-05-18 / Post Phase 6 / 激活剩余最小工具定义

### 当前目标

让写入类和 shell 类工具的定义状态与真实最小实现对齐。

### 本次完成

- 将 `write_file` 标记为 `active`
- 将 `patch_file` 标记为 `active`
- 将 `shell_exec` 标记为 `active`

### 当前状态

- 已完成：最小工具集都已进入 active 状态
- 进行中：参数 schema 仍偏简化
- 未完成：更细粒度 schema 和执行能力描述

### 风险与阻塞

- 当前工具定义还不能完整表达复杂命令或补丁格式

### 下一步

- 在后续阶段继续细化 schema 和工具能力边界
