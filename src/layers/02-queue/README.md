# 02 Queue

队列层，负责任务排队、取出和状态维护。

## 目标

提供最小、稳定、可观察的任务排队能力。

## 职责

- 任务入队
- 任务出队
- 维护任务状态
- 保持 FIFO 顺序

## 不负责

- 不管理线程池
- 不理解任务业务语义
- 不调用 Harness
- 不做并发调度

## 当前阶段

MVP 默认采用内存 FIFO 队列，后续实现也必须保持职责单一。
