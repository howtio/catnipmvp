# 04 Harness

运行编排层，负责一次 Agent run 的生命周期管理。

## 目标

把上下文准备、技能注入、模型运行、验收和报告串成一个可追踪流程。

## 职责

- 创建 `runId`
- 管理 run 生命周期
- 调用 Context、Skills、Runner
- 执行验收检查
- 生成 final report
- 记录 run 级状态

## 不负责

- 不直接执行工具
- 不直接修改业务文件
- 不承载复杂模型推理

## 当前阶段

后续 run heartbeat 的观测和超时判断由本层感知，但事件传播不在本层处理。
