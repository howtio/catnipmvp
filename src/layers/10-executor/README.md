# 10 Executor

执行层，是系统中唯一允许产生真实副作用的边界。

## 目标

安全地执行工具，并把执行结果回传给系统。

## 职责

- 监听工具请求
- 执行权限、路径、命令 guard
- 执行真实副作用
- 记录审计日志

## 不负责

- 不做模型推理
- 不决定是否应该调用工具
- 不越权修改系统外资源

## 当前阶段

所有文件读写、shell、patch、git 相关副作用，后续都只能从本层发生。
