# 01 Gateway

入口层，负责把外部输入转换为可排队的标准任务。

## 目标

把用户输入安全、稳定地送入系统，不在本层做任何执行决策。

## 职责

- 接收 CLI 或其他入口输入
- 校验输入格式与最小必需参数
- 构造标准 `RunTask`
- 把任务提交给 Queue

## 不负责

- 不调用模型
- 不执行工具
- 不读写 workspace
- 不直接驱动 Harness 或 Executor

## 当前阶段

当前阶段以接口骨架和职责边界为主，不实现复杂入口逻辑。
